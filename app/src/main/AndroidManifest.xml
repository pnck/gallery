<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Fast-scroller scrub ticks on API < 34 (Compose SegmentTick covers 34+). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Permission matrix per PRD §6.3 -->
    <!-- API <= 32 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- API <= 29 (legacy model): write covers silent batch delete. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- API 33+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- API 34+ partial grant ("selected photos only") -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <!-- Special access (API 31+): silent batch deletion for free-up-space,
         granted by the user on a system settings page (no runtime prompt). -->
    <uses-permission android:name="android.permission.MANAGE_MEDIA" />

    <application
        android:name=".GalleryApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.ByosGallery"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- System media picker: other apps' ACTION_PICK / ACTION_GET_CONTENT
             (image|video) land on our local-media grid (ui/picker/PickerActivity). -->
        <activity
            android:name=".ui.picker.PickerActivity"
            android:exported="true"
            android:label="@string/picker_title">
            <intent-filter>
                <action android:name="android.intent.action.PICK" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.GET_CONTENT" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.OPENABLE" />
                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>

        <!-- Auth is device flow (ADR-0001): no redirect activity, no browser on
             device. Sign-in shows a code; the user approves on a second screen. -->

        <!-- Shares cacheDir originals (CLOUD_ONLY view cache, PRD §9.1) to
             ACTION_EDIT / ACTION_SEND targets via grant-URI permissions. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- On-demand WorkManager init: HiltWorkerFactory comes from GalleryApp
             (Configuration.Provider), so the default initializer must go. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
