<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permission matrix per PRD §6.3 -->
    <!-- API <= 32 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- API 33+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- API 34+ partial grant ("selected photos only") -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <application
        android:name=".GalleryApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ByosGallery"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AppAuth redirect capture (PRD §5.2). The scheme comes from
             manifestPlaceholders["appAuthRedirectScheme"] in build.gradle.kts;
             AppAuth's RedirectUriReceiverActivity is contributed by the library
             manifest and picks the placeholder up automatically. This activity
             receives the completion intent and finishes the token exchange. -->
        <activity
            android:name=".auth.OAuthCallbackActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- On-demand WorkManager init: HiltWorkerFactory comes from GalleryApp
             (Configuration.Provider), so the default initializer must go. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
